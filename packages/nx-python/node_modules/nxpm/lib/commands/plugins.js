"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const plugins_1 = require("../lib/plugins/plugins");
const utils_1 = require("../utils");
class Plugins extends utils_1.BaseCommand {
    async run() {
        const { flags } = this.parse(Plugins);
        await plugins_1.plugins({
            cwd: flags.cwd,
            userConfig: this.userConfig,
            refresh: flags.refresh,
            config: this.config,
        });
    }
}
exports.default = Plugins;
Plugins.aliases = ['pl'];
Plugins.description = 'Install and remove community plugins';
Plugins.flags = {
    cwd: command_1.flags.string({
        char: 'c',
        description: 'Current working directory',
        default: process.cwd(),
    }),
    help: command_1.flags.help({ char: 'h' }),
    refresh: command_1.flags.boolean({
        char: 'r',
        description: 'Refresh the list of plugins',
        default: false,
    }),
};
