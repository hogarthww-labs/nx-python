"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const config_1 = require("../../lib/config/config");
const utils_1 = require("../../utils");
class ConfigDelete extends utils_1.BaseCommand {
    async run() {
        const { flags } = this.parse(ConfigDelete);
        await config_1.deleteConfig({
            global: flags.global,
            userConfig: this.userConfig,
            config: this.config,
        });
    }
}
exports.default = ConfigDelete;
ConfigDelete.description = 'Delete the config file';
ConfigDelete.flags = {
    help: command_1.flags.help({ char: 'h' }),
    global: command_1.flags.boolean({ char: 'g', description: 'Global config', required: true }),
};
ConfigDelete.args = [];
