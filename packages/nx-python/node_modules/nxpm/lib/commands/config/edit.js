"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const config_1 = require("../../lib/config/config");
const utils_1 = require("../../utils");
class ConfigEdit extends utils_1.BaseCommand {
    async run() {
        const { flags } = this.parse(ConfigEdit);
        await config_1.editConfig({
            global: flags.global,
            userConfig: this.userConfig,
            config: this.config,
        });
    }
}
exports.default = ConfigEdit;
ConfigEdit.description = 'Edit the config file';
ConfigEdit.flags = {
    help: command_1.flags.help({ char: 'h' }),
    global: command_1.flags.boolean({ char: 'g', description: 'Global config', required: true }),
};
ConfigEdit.args = [];
