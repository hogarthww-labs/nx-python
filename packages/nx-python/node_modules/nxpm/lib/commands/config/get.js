"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const config_1 = require("../../lib/config/config");
const utils_1 = require("../../utils");
class ConfigGet extends utils_1.BaseCommand {
    async run() {
        const { args, flags } = this.parse(ConfigGet);
        await config_1.getConfigParam({
            global: flags.global,
            key: args.key,
            userConfig: this.userConfig,
            config: this.config,
        });
    }
}
exports.default = ConfigGet;
ConfigGet.description = 'describe the command here';
ConfigGet.flags = {
    help: command_1.flags.help({ char: 'h' }),
    global: command_1.flags.boolean({ char: 'g', description: 'Global config', required: true }),
};
ConfigGet.args = [
    {
        name: 'key',
        required: true,
    },
];
