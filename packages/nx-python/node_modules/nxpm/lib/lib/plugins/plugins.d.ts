import { WorkspaceInfo } from '../../utils';
import { SchematicCollection } from '../../utils/vendor/nx-console/schema';
import { PluginConfig } from './interfaces/plugin-config';
export interface NxPlugin {
    name: string;
    description: string;
    url: string;
}
export declare const loadPluginsSchematics: (info: WorkspaceInfo, config: PluginConfig) => Promise<{
    plugins: any[];
    schematics: SchematicCollection[];
    schematicsNames: string[];
    availablePlugins: any[];
    availablePluginNames: string[];
    installedPlugins: any[];
    installedPluginNames: string[];
    pluginNames: string[];
}>;
export declare const selectPlugin: (plugins: any[], message: string) => Promise<false | {
    pluginName: string;
}>;
export declare const selectPluginFlow: (info: WorkspaceInfo, config: PluginConfig, pluginName?: string | undefined) => Promise<false | {
    selection: string;
    pluginName: string;
    plugin?: NxPlugin | undefined;
}>;
export declare const plugins: (config: PluginConfig) => Promise<void>;
