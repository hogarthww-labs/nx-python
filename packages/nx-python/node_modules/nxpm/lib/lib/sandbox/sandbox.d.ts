import { Sandbox } from './interfaces/sandbox';
import { SandboxConfig } from './interfaces/sandbox-config';
export interface NxPlugin {
    name: string;
    description: string;
    url: string;
}
export declare const selectSandbox: (sandboxes: any[], message: string) => Promise<false | {
    sandboxName: string;
}>;
export declare const getConfigAction: (config: SandboxConfig) => "[ RUN ]" | undefined;
export declare const selectSandboxFlow: (config: SandboxConfig, sandboxName?: string | undefined) => Promise<false | {
    selection: string;
    sandboxName: string;
    sandbox?: Sandbox | undefined;
}>;
export declare const sandbox: (config: SandboxConfig) => Promise<void>;
