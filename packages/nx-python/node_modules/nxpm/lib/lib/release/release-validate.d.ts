import { ReleaseConfig } from './interfaces/release-config';
import { ValidatedConfig } from './interfaces/validated-config';
import { ValidatedPackages } from './interfaces/validated-packages';
import { ValidatedWorkspace } from './interfaces/validated-workspace';
export declare function validateConfig(config: ReleaseConfig): ValidatedConfig | false;
export declare function validateWorkspace(config: ValidatedConfig): ValidatedWorkspace | false;
export declare function validatePackages(config: ValidatedConfig, workspace: ValidatedWorkspace): ValidatedPackages | false;
